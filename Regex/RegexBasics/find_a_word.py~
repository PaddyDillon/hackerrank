import re

def convert_string(s):
     j=re.compile( "s[^a-zA-Z0-9]+" )
     return j
def test_string(s,sen):
    regex=convert_string(s)
    return len(regex.findall(sen))

    
    


num_sen=int(input())
sentences=[]
for i in range(num_sen):
    sentences.append(input())


num_tests=int(input())
for i in range(num_tests):
    
